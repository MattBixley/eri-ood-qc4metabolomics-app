# Job submission configuration file
#
---

#
# Configure the content of the job script for the batch job here
# @see http://www.rubydoc.info/gems/ood_core/OodCore/BatchConnect/Template
#
batch_connect:
  # We use the basic web server template for generating the job script
  #
  # @note Do not change this unless you know what you are doing!
  template: "basic"

  # You can override the command used to query the hostname of the compute node
  # here
  #
  # @note It is **highly** recommended this be set in the global cluster
  #   configuration file so that all apps can take advantage of it by default
  #
  #set_host: "host=$(hostname -A | awk '{print $2}')"

#
# Configure the job script submission parameters for the batch job here
# @see http://www.rubydoc.info/gems/ood_core/OodCore/Job/Script
#
script:
  job_name: "qc4metabolomics"
  native:
    # num_cores/num_mem/partition all come from the form (form.yml).
    # --exclusive still claims the whole node regardless of num_cores/
    # num_mem (needed so the stack's shared host-network namespace has no
    # port clashes with another job) - so those two only affect what SLURM
    # records as requested for the job, not what's actually usable.
    # `partition` is a plain select, not a bc_queue smart attribute, so it's
    # the single, explicit source of truth for which partition gets used -
    # no separate queue->partition mapping to redundantly (or worse,
    # conflictingly) also apply.
    - <%= "--nodes=1" %>
    - <%= "--cpus-per-task=#{num_cores}" %>
    - <%= "--mem=#{num_mem}gb" %>
    - "--exclusive"
    - <%= "--partition=#{partition}" %>
